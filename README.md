# CIFAR-classificationCNN-keras
A simple CNN model is created in <code>keras</code> and it is then used to classify images in the CIFAR10 dataset. Resluts and outputs are also included in the notebook.
